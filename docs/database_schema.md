Database Schema (local-first)

- Core tables / stores (logical, pluggable per backend)
- MemoryObjects (MemPalace units)
- EmbeddingIndex
- GraphNodes
- GraphEdges
- BeliefState
- AgentOutputs
- AuditLog

- Relationships and indexing
- Example entity definitions follow with JSON schema-like references (not code).
